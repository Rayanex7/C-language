#include<stdio.h>
int main(){
float Lar, Long, Sur, Per;
	printf("Entrer la largeur du rectangle : ");
	scanf("%f", &Lar);
	printf("Entrer la longueur du rectangle : ");
        scanf("%f", &Long);
		Sur = Lar * Long;
		Per = (Lar + Long)*2;
	printf("La surface du rectangle est : %.2f\nSon perimetre est : %.2f\n", Sur, Per);
	















return 0;
}
